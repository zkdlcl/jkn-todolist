상단 탑 영역 토큰 값을 기준으로 store에 담아뒀다가 top화면에 뿌리는데 이 때 토큰값에 user의 name을 포함하지 않아
메일 앞 부분 @잘라서 앞부분을 썼다가 다시 name을 db로부터 토큰에 심어서 처리 함

이후 최초 로그인 시는 한글을 잘 표현하다가 이후 메일로 돌아갔는데, 이부분은 한글 디코딩 영역이 필요하여, 디코딩하고 다른곳에 잡는 것으로 처리해서 수정
